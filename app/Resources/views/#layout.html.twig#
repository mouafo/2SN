<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
	{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
	Connecteœô¥Á en tant que {{ app.user.username }} - <a href="{{
								 path('fos_user_security_logout') }}">Deœô¥Áconnexion</a> {%else%}<a href="{{ path('fos_u
</body>
</html>
